Next steps (agentically where possible)

Deferred from this batch (need their own pass):
- MP sniper / artillery aim indicators not synced. Needs new wire-format additions (signal events for aim start / fire) and matching receiver visuals on peers.
- Shop lock badges. Right-click lock is wired (turret / rune / mod slots) and locks persist through reroll, but the locked slots don't render a visible "L" / tint yet.
